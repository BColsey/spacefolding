{
  "name": "spacefolding",
  "version": "0.1.0",
  "description": "Spacefolding — intelligent context compression and routing for coding agents",
  "type": "module",
  "main": "dist/main.js",
  "bin": {
    "spacefolding": "dist/main.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "start": "node dist/main.js serve",
    "dev": "tsx src/main.ts serve",
    "lint": "tsc --noEmit",
    "serve": "node dist/main.js serve"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.1.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "better-sqlite3": "^11.0.0",
    "chalk": "^5.3.0",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.9",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
