# Spacefolding Configuration
NODE_ENV=production
DB_PATH=/app/data/spacefolding.db
MODEL_PATH=/app/data/models
EMBEDDING_PROVIDER=local
EMBEDDING_MODEL=Xenova/all-MiniLM-L6-v2
COMPRESSION_PROVIDER=deterministic
TRANSPORT=stdio
PORT=3000
WEB_PORT=8080
USE_GPU=0
